repos:
  - repo: local
    hooks:
      - id: guard-commit
        name: Guard commit (email + work hours)
        entry: bash scripts/guard-commit.sh
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-commit]
